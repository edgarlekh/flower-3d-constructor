# Зависимости пайплайна моделей (scripts/). НЕ нужны для сайта — сайт чистый фронтенд.
# Установка:  pip install -r scripts/requirements.txt

numpy>=1.24        # квантование геометрии
trimesh>=4.0       # загрузка GLB, нормали, UV
scipy>=1.10        # trimesh использует для расчёта нормалей вершин
pillow>=10.0       # сжатие текстур, сэмплинг цвета в вершины
requests>=2.31     # обращение к API генерации моделей (Meshy)
