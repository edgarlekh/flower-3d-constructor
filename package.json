{
  "name": "flower-3d-constructor",
  "private": true,
  "version": "0.0.0",
  "description": "Тесты чистой геометрии плана зала (assets/js/plan-geometry.js). Без сборки и npm-зависимостей.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
